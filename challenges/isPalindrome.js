/**
 * Level 1: Begineer - Palindrome Checker
 * Write a function that takes a string as input and returns true
 * if it is a palindrome (reads the same forwards and backwards),
 * and false otherwise. Ignore spaces and letter casing.
 *
 * Example:
 * isPalindrome("A man a plan a canal Panama") should return true.
 */
export const isPalindrome = (str) => {
  // Your code here
};
