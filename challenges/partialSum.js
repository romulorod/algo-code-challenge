/**
 * Level 1: Begineer - Partial Sum
 *
 * You have to write a function so you can pass the sum parameters
 * inside the parenthesis
 *
 * Example:
 * partialSum(10)(20) // Should return 30
 *
 */
export const partialSum = (num) => {
  // Your Code Goes Here
};
