/**
 * Level 2: Intermediate
 * Implement the Bubble Sort algorithm, which sorts an array of elements
 * in ascending order. This process should be repeated until the entire
 * array is sorted, with the largest elements "bubbling" to the end of
 * the array.
 *
 * The array can only contain numbers
 *
 */
export const bubbleSort = (arr) => {
  // Your Code Goes Here
};
