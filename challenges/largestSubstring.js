/**
 * Level 3: Advanced - Longest Substring Without Repeating Characters
 * Given a string, find the length of the longest substring without
 * repeating characters.
 * Example: For the input "abcabcbb", the longest substring without
 * repeating characters is "abc", so the function should return 3.
 *
 */
export const longestSubstringWithoutRepeatingChars = (str) => {
  // Your Code Goes Here
};
